 ;(function ($){
	 // init bPopup
	 $('._js-btn-open-modal').on('click', function (){
	 	 $('#modal-order').bPopup({
	 	 	closeClass:'modal-сlose',
	 	 });
	 });
	// init fromstaller
	 $('select').styler();
 })(jQuery);
